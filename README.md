# news-roundup

```bash
docker compose run --rm --build news-roundup
```

```
use news roundup skill with tiny language models (less than 4b parameters) 

use news roundup skill with tiny language models (less than 10b parameters) 
```


